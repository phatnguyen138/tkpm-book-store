export type Genre  = {
    genre_id: number;
    name: string;
    image: any;
}

export type GenreInfo = {
    genre_id: number;
    name: string;
}

export type NewGenre  = {
    name: string
}

export type EachGenre = {
    genre_id: number;
    name: string;
    image: any;
}